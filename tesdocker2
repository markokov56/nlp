pipeline {
    agent {
        docker { image 'python' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'python --version'
                echo "executing python script api twitter"
                echo "executing python script api twitter OK"   
            }
        }
    }
}
