pipeline {
    agent {
        docker { image 'python' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'python --version'
                sh 'pip install requests --user'
                echo "executing python script api twitter"
                sh 'python apitwitter_git.py'
            }
        }
    }
}
